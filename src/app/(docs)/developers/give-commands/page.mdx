export const metadata = {
  title: "Give commands and carriers",
  description:
    "The full grammar: every carrier kind against every Minecraft command era.",
};

# Give commands and carriers

{/* The type pages show *a* command; this page shows the grammar. Say that
    in one line so a reader knows which page they want. */}

## The three eras

{/* Modern item definitions, the components era, and legacy NBT — which
    Minecraft versions each covers. */}

## Item carrier

{/* The custom_model_data string form, and what each string means. */}

## Full block carrier

{/* block_state form, per era. */}

## Partial block carrier

{/* Same, plus the directional/fixed split. */}

## Reading it from an export

{/* How to find the right command for a given model without guessing —
    studio shows it, and the single-model export builds it. */}
