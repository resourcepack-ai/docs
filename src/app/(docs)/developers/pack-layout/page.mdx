export const metadata = {
  title: "Pack layout reference",
  description:
    "The file tree of an exported pack: what each directory holds and which files are bookkeeping.",
};

# Pack layout reference

{/* Reference, not a guide — assume the reader has a zip open. */}

## The tree

```
TODO
```

## pack.mcmeta

{/* pack_format, the min/max form on newer versions, description. */}

## Where each asset kind lands

{/* Textures, models, the font provider. One row each. */}

## Bookkeeping files

{/* Which files aren't assets and shouldn't be edited or counted. */}

<Callout type="note">
  {/* Document shape, not values — anything a reader can read off their own
      export doesn't belong in a table here. */}
</Callout>
