export const metadata = {
  title: "Bedrock and Geyser",
  description:
    "The .mcpack export: what converts from the Java pack, what doesn't, and how Geyser players receive it.",
};

# Bedrock and Geyser

{/* The one destination that cuts across every asset type, which is why it's
    a page and not a fifth section on each. */}

## The .mcpack

{/* What it is, how it's built from the same assets, and how a player
    installs one by hand. */}

## What converts

{/* Per asset kind — textures, geometry, animations, icons. A table is
    probably clearest. */}

## What doesn't

{/* Be specific and honest. Carriers, give commands, anything Java-only. */}

## Through Geyser

{/* Linked Bedrock players, the transfer-reconnect, and why applying a pack
    means a rejoin. Link /testing/your-server rather than repeating it. */}
