export const metadata = {
  title: "Exporting textures",
  description:
    "What a texture becomes in an exported pack, and how it behaves in vanilla, with a plugin, and on Bedrock.",
};

# Exporting textures

{/* One paragraph: a texture is the simplest export — it replaces a vanilla
    path and needs nothing to work. Set that expectation up front. */}

## In vanilla

{/* Where the PNG lands, which vanilla texture it overrides, resolution and
    the render-type/transparency caveat. */}

## With our plugin

{/* Almost nothing type-specific — pushing a pack applies textures like any
    other asset. Keep it short and link to /testing/your-server. */}

## In your own plugin

{/* Serving the pack yourself: setResourcePack with a URL and hash, and the
    fact that textures need no per-asset code at all. */}

## On Bedrock

{/* What converts, what doesn't, and the path/naming differences. */}

<CardGroup>
  {/* Texture editor, /developers/pack-layout */}
</CardGroup>
