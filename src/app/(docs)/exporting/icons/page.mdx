export const metadata = {
  title: "Exporting icons",
  description:
    "Typing an exported glyph into chat, signs and books — by hand, from a command, or from your own plugin.",
};

# Exporting icons

{/* An icon is a font glyph at a private-use codepoint. Once the pack is on,
    typing that character shows the art anywhere Minecraft renders text. */}

## Finding an icon's character

{/* How to get the codepoint out of studio. Do NOT hardcode a table of
    assignments here — they're per pack and will drift. */}

## In vanilla

{/* Paste into chat, a sign, a book. What renders and what doesn't. */}

## With our plugin

{/* Whatever's genuinely different when the pack arrives via a push —
    probably very little. Keep it short. */}

## In your own plugin

{/* The useful one: an Adventure Component carrying the codepoint, and the
    escaping trap in plain strings / config files. */}

```java
// TODO
```

## On Bedrock

{/* Whether the glyph survives the .mcpack conversion, and the limits. */}

<CardGroup>
  {/* Icons editor, sample code */}
</CardGroup>
