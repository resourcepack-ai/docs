export const metadata = {
  title: "Exporting animations",
  description:
    "Why animations sit static in a Java export, play with the plugin, and convert natively on Bedrock.",
};

# Exporting animations

{/* The headline is the asymmetry: Java stores them as extra fields nothing
    reads, Bedrock turns them into real animations. Say it in the first
    paragraph, not in a callout at the bottom. */}

## In vanilla

{/* Nothing plays. Why the pack is still valid (vanilla ignores unknown
    fields), and what the model looks like instead. */}

<Callout type="warning">
  {/* The "your animation will not play without a plugin" warning. */}
</Callout>

## With our plugin

{/* Server-driven playback, triggers, what the plugin reads out of the
    model JSON. Link /editors/animations for authoring. */}

## In your own plugin

{/* Where the keyframe data lives in the exported JSON and what you'd have
    to drive yourself to play it. Be honest about the effort here. */}

```java
// TODO
```

## On Bedrock

{/* Native keyframe animation, client-side playback, and why it's smoother
    than the Java path. */}

<CardGroup>
  {/* Animations editor, exporting models */}
</CardGroup>
