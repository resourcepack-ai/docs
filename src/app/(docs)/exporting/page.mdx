export const metadata = {
  title: "Exporting a pack",
  description:
    "The three things you can export, what lands in the file, and which Minecraft versions it targets.",
};

# Exporting a pack

{/* What export is for, and that you don't need it to test. Point at the
    three exports below rather than explaining them here. */}

## What you can export

{/* Whole pack -> Java .zip. Whole pack -> Bedrock .mcpack. One model -> .zip.
    A short table is probably right: what it contains, when you'd pick it. */}

## Choosing a Minecraft version

{/* Only the single-model export asks. The version picker, the minVersion
    floor and why a model can be refused for a version below it. */}

## What's inside the file

{/* The file tree of a real export, one level deep. Full reference lives in
    /developers/pack-layout — link, don't repeat. */}

## Installing it

{/* Singleplayer resourcepacks folder; handing it to a server. The plugin
    path is a link out, not a section. */}

<CardGroup>
  {/* Cards to the per-type pages. */}
</CardGroup>
