export const metadata = {
  title: "Exporting GUIs",
  description:
    "What a GUI texture replaces in an exported pack, and the limits of what a pack can change about a screen.",
};

# Exporting GUIs

{/* GUIs are draw-only — a pack changes how a screen looks, not what it
    does or where its slots are. Lead with that limit, it shapes the rest. */}

## In vanilla

{/* Which vanilla screen textures can be replaced, and what a player sees. */}

## With our plugin

{/* Short. Nothing GUI-specific about a push. */}

## In your own plugin

{/* Opening an inventory whose title/size lines up with the replaced
    texture, since that's the part a plugin actually controls. */}

```java
// TODO
```

## On Bedrock

{/* Be honest about how little of this crosses over. */}

<CardGroup>
  {/* Pack layout, sample code */}
</CardGroup>
