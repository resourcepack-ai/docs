export const metadata = {
  title: "Exporting models",
  description:
    "Carriers, give commands and placement — how an exported model becomes something you can hold and put down.",
};

# Exporting models

{/* The one that needs the most explaining. Lead with why: a pack can't add
    a block, so every model rides a carrier, and the carrier decides
    everything below. */}

## Carriers in an export

{/* Item vs full block vs partial block, as it affects the exported files —
    not a re-run of /packs. What each one writes into the zip. */}

## In vanilla

{/* Block carriers work with no plugin: place the vanilla block, get the
    model. Item carriers need a give command. Where the ceiling is. */}

## With our plugin

{/* Push, /give, place, punch to remove. Mostly links to
    /testing/placing-models — say what's different about an *exported*
    model rather than repeating that page. */}

## In your own plugin

{/* The real code section. Building the carrier ItemStack with
    custom_model_data, and spawning the display entity for a placement.
    Full era table lives in /developers/give-commands. */}

```java
// TODO
```

## Animated models

{/* The asymmetry, and it's the headline of this section: a Java export
    stores keyframes as extra fields nothing reads, so the model sits
    static and playback needs a plugin. Bedrock converts them into real
    animations. Link /editors/animations for authoring. */}

<Callout type="warning">
  {/* Your animation will not play from a Java export alone. */}
</Callout>

## On Bedrock

{/* Geometry converts natively; the carrier concept doesn't exist the same
    way. What a model is on that side, what Geyser does with it, and
    client-side animation playback. */}

<CardGroup>
  {/* Model editor, placing models, give commands reference */}
</CardGroup>
